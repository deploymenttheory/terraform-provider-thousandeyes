// Code generated by tfpfgen from blueprints/thousandeyes
// (sha256:d986d7900b3714639519883166d0066510af34e4e4431384031a6f7b9dc0895a). DO NOT EDIT.

package alerts_rule_test

import (
	"os"
	"path/filepath"
	"testing"
	"time"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"

	"github.com/deploymenttheory/terraform-provider-thousandeyes/internal/acceptance"
	"github.com/deploymenttheory/terraform-provider-thousandeyes/internal/acceptance/check"
	"github.com/deploymenttheory/terraform-provider-thousandeyes/internal/acceptance/destroy"
	"github.com/deploymenttheory/terraform-provider-thousandeyes/internal/services/datasources/alert_rules/v7/alerts_rule"
)

// The data source under test, and the seed resource whose apply produced the object it
// reads. The only thing this test creates is the seed, so existence and destroy are
// verified through the seed's own helper.
const (
	address     = "data.thousandeyes_alerts_rule.test"
	seedAddress = "thousandeyes_alerts_rule.test"
)

var seedResource = alerts_rule.AlertsRuleTestResource{}

// TestAccDataSourceAlertsRule_01_Read seeds one object and reads it back through the data source.
//
// Every cross-key check compares the data source's state against the state the seed's
// apply produced. That is the point of the test: the two arrive by different code paths
// -- the resource's create-then-read against the data source's read -- and a flatten
// that drops or mangles a field on either path shows up here as a mismatch, which no
// amount of compiling can see.
func TestAccDataSourceAlertsRule_01_Read(t *testing.T) {
	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { acceptance.PreCheck(t) },
		ProtoV6ProviderFactories: acceptance.ProtoV6ProviderFactories,
		// hashicorp/time supplies the propagation wait the fixture declares between
		// creating the seed and reading it back.
		ExternalProviders: map[string]resource.ExternalProvider{
			"time": {Source: "hashicorp/time", VersionConstraint: ">= 0.14.0"},
		},
		// The data source owns nothing; destroy verification is the seed's.
		//
		// The wait is the built-in default; this API's consistency after delete was never measured,
		// so this is a budget rather than an observation
		CheckDestroy: destroy.CheckDestroyed(seedResource, "thousandeyes_alerts_rule", 20*time.Second),
		Steps: []resource.TestStep{
			{
				Config: config(t, "datasource.tf"),
				Check: resource.ComposeAggregateTestCheckFunc(
					check.That(address).Key("alert_group_type").MatchesOtherKey(check.That(seedAddress).Key("alert_group_type")),
					check.That(address).Key("alert_type").MatchesOtherKey(check.That(seedAddress).Key("alert_type")),
					check.That(address).Key("description").MatchesOtherKey(check.That(seedAddress).Key("description")),
					check.That(address).Key("direction").MatchesOtherKey(check.That(seedAddress).Key("direction")),
					check.That(address).Key("expression").MatchesOtherKey(check.That(seedAddress).Key("expression")),
					check.That(address).Key("id").MatchesOtherKey(check.That(seedAddress).Key("id")),
					check.That(address).Key("include_covered_prefixes").MatchesOtherKey(check.That(seedAddress).Key("include_covered_prefixes")),
					check.That(address).Key("is_default").MatchesOtherKey(check.That(seedAddress).Key("is_default")),
					check.That(address).Key("minimum_sources").MatchesOtherKey(check.That(seedAddress).Key("minimum_sources")),
					check.That(address).Key("minimum_sources_pct").MatchesOtherKey(check.That(seedAddress).Key("minimum_sources_pct")),
					check.That(address).Key("notify_on_clear").MatchesOtherKey(check.That(seedAddress).Key("notify_on_clear")),
					check.That(address).Key("rounds_violating_mode").MatchesOtherKey(check.That(seedAddress).Key("rounds_violating_mode")),
					check.That(address).Key("rounds_violating_out_of").MatchesOtherKey(check.That(seedAddress).Key("rounds_violating_out_of")),
					check.That(address).Key("rounds_violating_required").MatchesOtherKey(check.That(seedAddress).Key("rounds_violating_required")),
					check.That(address).Key("rule_name").MatchesOtherKey(check.That(seedAddress).Key("rule_name")),
					check.That(address).Key("sensitivity_level").MatchesOtherKey(check.That(seedAddress).Key("sensitivity_level")),
					check.That(address).Key("severity").MatchesOtherKey(check.That(seedAddress).Key("severity")),
				),
			},
		},
	})
}

// config reads the committed fixture; see the resource acceptance test for why testdata.
func config(t *testing.T, name string) string {
	t.Helper()

	body, err := os.ReadFile(filepath.Join("testdata", name))
	if err != nil {
		t.Fatalf("reading fixture %s: %v", name, err)
	}

	return string(body)
}
