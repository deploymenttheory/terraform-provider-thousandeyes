// Code generated by tfpfgen from blueprints/thousandeyes
// (sha256:17ccadd9f04833a0c47aa480004eff43ce664c1e4202c4da5c92d766e70cb2c7). DO NOT EDIT.

// REF: https://developer.cisco.com/docs/thousandeyes/disable-endpoint-agent/
package disable_endpoint_agent

import (
	"context"

	"github.com/hashicorp/terraform-plugin-framework/action"
	"github.com/hashicorp/terraform-plugin-framework/action/schema"

	"github.com/deploymenttheory/terraform-provider-thousandeyes/internal/client"
	sdk "github.com/deploymenttheory/terraform-provider-thousandeyes/internal/sdk"
)

const (
	// TypeName is the Terraform type this action registers as.
	TypeName = "thousandeyes_disable_endpoint_agent"

	// InvokeTimeout is the deadline in seconds. One operation, one deadline -- and unlike a
	// resource there is no timeouts block to override it, because the framework's action
	// schema has no home for one.
	InvokeTimeout = 180
)

// Compile-time assertions that this action implements what it claims to.
var (
	_ action.Action              = (*DisableEndpointAgentAction)(nil)
	_ action.ActionWithConfigure = (*DisableEndpointAgentAction)(nil)
)

// NewDisableEndpointAgentAction returns the thousandeyes_disable_endpoint_agent action.
func NewDisableEndpointAgentAction() action.Action {
	return &DisableEndpointAgentAction{}
}

// DisableEndpointAgentAction implements the thousandeyes_disable_endpoint_agent action.
//
// An action has no state. It reads configuration, calls the API once and reports what
// happened, which is why there is no model to reconcile and no flatten step: InvokeRequest
// carries only a config and InvokeResponse has no field to return a value in.
type DisableEndpointAgentAction struct {
	client *sdk.ThousandEyesClient
}

// Metadata returns the action type name.
func (a *DisableEndpointAgentAction) Metadata(_ context.Context, _ action.MetadataRequest, resp *action.MetadataResponse) {
	resp.TypeName = TypeName
}

// Configure receives the configured SDK client from the provider.
//
// action.ConfigureRequest, which is its own type: the framework gives each block kind a
// distinct one, so this cannot share the resource's helper.
func (a *DisableEndpointAgentAction) Configure(ctx context.Context, req action.ConfigureRequest, resp *action.ConfigureResponse) {
	a.client = client.ForAction(ctx, req, resp, TypeName)
}

// Schema returns the action schema.
//
// Every attribute is required or optional. An action attribute cannot be Computed or
// Sensitive -- there is no state for a computed value to live in, and no stored value to mark
// sensitive -- which blueprint.Validate refuses rather than emitting a field that does not
// exist on the action schema types.
func (a *DisableEndpointAgentAction) Schema(ctx context.Context, _ action.SchemaRequest, resp *action.SchemaResponse) {
	resp.Schema = schema.Schema{
		MarkdownDescription: "Disables a ThousandEyes endpoint agent. An action rather than a resource: it is a one-shot operation with nothing for Terraform to reconcile, and the agent's enabled state is not something this manages over time.",
		Attributes: map[string]schema.Attribute{
			"agent_id": schema.StringAttribute{
				Required: true,
				MarkdownDescription: "The identifier of the endpoint agent to disable. Disabling an agent stops it running tests " +
					"without deleting it, so the operation is reversible by the matching enable action.",
			},
		},
	}
}
