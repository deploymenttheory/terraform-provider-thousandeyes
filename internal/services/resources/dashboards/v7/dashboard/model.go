// Code generated by tfpfgen from blueprints/thousandeyes
// (sha256:17ccadd9f04833a0c47aa480004eff43ce664c1e4202c4da5c92d766e70cb2c7). DO NOT EDIT.

package dashboard

import (
	"github.com/hashicorp/terraform-plugin-framework-timeouts/resource/timeouts"
	"github.com/hashicorp/terraform-plugin-framework/attr"
	"github.com/hashicorp/terraform-plugin-framework/types"
)

// DashboardResourceModel is the Terraform state for a thousandeyes_dashboard.
//
// Field order follows the schema, and the timeouts value is last.
type DashboardResourceModel struct {
	AccountID             types.Int64    `tfsdk:"account_id"`
	AID                   types.String   `tfsdk:"aid"`
	CreatedBy             types.Int64    `tfsdk:"created_by"`
	DashboardCreatedBy    types.String   `tfsdk:"dashboard_created_by"`
	DashboardModifiedBy   types.String   `tfsdk:"dashboard_modified_by"`
	DashboardModifiedDate types.String   `tfsdk:"dashboard_modified_date"`
	Description           types.String   `tfsdk:"description"`
	GlobalFilterID        types.String   `tfsdk:"global_filter_id"`
	GlobalOverride        types.Bool     `tfsdk:"global_override"`
	ID                    types.String   `tfsdk:"id"`
	IsBuiltIn             types.Bool     `tfsdk:"is_built_in"`
	IsDefaultForAccount   types.Bool     `tfsdk:"is_default_for_account"`
	IsDefaultForUser      types.Bool     `tfsdk:"is_default_for_user"`
	IsGlobalOverride      types.Bool     `tfsdk:"is_global_override"`
	IsMigratedReport      types.Bool     `tfsdk:"is_migrated_report"`
	IsPrivate             types.Bool     `tfsdk:"is_private"`
	Layout                types.Object   `tfsdk:"layout"`
	MigratedReport        types.Bool     `tfsdk:"migrated_report"`
	ModifiedBy            types.Int64    `tfsdk:"modified_by"`
	ModifiedDate          types.String   `tfsdk:"modified_date"`
	RefreshRate           types.String   `tfsdk:"refresh_rate"`
	Title                 types.String   `tfsdk:"title"`
	Timeouts              timeouts.Value `tfsdk:"timeouts"`
}

// DashboardLayoutModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type DashboardLayoutModel struct {
	Details  types.Object `tfsdk:"details"`
	LayoutID types.String `tfsdk:"layout_id"`
	Type     types.String `tfsdk:"type"`
}

// dashboardLayoutAttrTypes describes DashboardLayoutModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var dashboardLayoutAttrTypes = map[string]attr.Type{
	"details":   dashboardLayoutDetailsObjectType,
	"layout_id": types.StringType,
	"type":      types.StringType,
}

var dashboardLayoutObjectType = types.ObjectType{AttrTypes: dashboardLayoutAttrTypes}

// DashboardLayoutDetailsModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type DashboardLayoutDetailsModel struct {
	WidgetPositioning types.Set `tfsdk:"widget_positioning"`
}

// dashboardLayoutDetailsAttrTypes describes DashboardLayoutDetailsModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var dashboardLayoutDetailsAttrTypes = map[string]attr.Type{
	"widget_positioning": types.SetType{ElemType: dashboardWidgetPositionObjectType},
}

var dashboardLayoutDetailsObjectType = types.ObjectType{AttrTypes: dashboardLayoutDetailsAttrTypes}

// DashboardWidgetPositionModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type DashboardWidgetPositionModel struct {
	H  types.Int64  `tfsdk:"h"`
	ID types.String `tfsdk:"id"`
	W  types.Int64  `tfsdk:"w"`
	X  types.Int64  `tfsdk:"x"`
	Y  types.Int64  `tfsdk:"y"`
}

// dashboardWidgetPositionAttrTypes describes DashboardWidgetPositionModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var dashboardWidgetPositionAttrTypes = map[string]attr.Type{
	"h":  types.Int64Type,
	"id": types.StringType,
	"w":  types.Int64Type,
	"x":  types.Int64Type,
	"y":  types.Int64Type,
}

var dashboardWidgetPositionObjectType = types.ObjectType{AttrTypes: dashboardWidgetPositionAttrTypes}
