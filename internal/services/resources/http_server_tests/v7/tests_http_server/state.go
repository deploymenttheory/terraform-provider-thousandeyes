// Code generated by tfpfgen from blueprints/thousandeyes
// (sha256:c2f31b5ceae54901082d303a3578916d3c0f177b0c43d44a1e482ca80a1e9dd1). DO NOT EDIT.

package tests_http_server

import (
	"context"

	"github.com/hashicorp/terraform-plugin-framework/diag"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-log/tflog"

	models "github.com/deploymenttheory/terraform-provider-thousandeyes/internal/sdk/models"
	"github.com/deploymenttheory/terraform-provider-thousandeyes/internal/services/common/convert"
)

// mapRemoteStateToTerraform maps the API's response onto Terraform state.
//
// This function replaces what a reflection-based provider does at runtime. Being
// generated code, a field whose conversion is wrong is a compile error rather
// than a surprise during apply.
func mapRemoteStateToTerraform(ctx context.Context, data *TestsHTTPServerResourceModel, remote models.Tests_API_HttpServerTestResponseable) diag.Diagnostics {
	var (
		diags diag.Diagnostics
		d     diag.Diagnostics
	)

	if remote == nil {
		tflog.Debug(ctx, "remote resource is nil, leaving state untouched", map[string]any{
			"resource": TypeName,
		})
		return diags
	}

	data.AlertsEnabled = convert.PtrBoolToFramework(remote.GetAlertsEnabled())
	data.AllowUnsafeLegacyRenegotiation = convert.PtrBoolToFramework(remote.GetAllowUnsafeLegacyRenegotiation())
	data.AuthType = convert.PtrStringerToFramework(remote.GetAuthType())
	data.BandwidthMeasurements = convert.PtrBoolToFramework(remote.GetBandwidthMeasurements())
	data.BGPMeasurements = convert.PtrBoolToFramework(remote.GetBgpMeasurements())
	data.ClientCertificate = convert.PtrEnumToFramework(remote.GetClientCertificate())
	data.CollectProxyNetworkData = convert.PtrBoolToFramework(remote.GetCollectProxyNetworkData())
	data.ContentRegex = convert.PtrStringToFramework(remote.GetContentRegex())
	data.CreatedBy = convert.PtrEnumToFramework(remote.GetCreatedBy())
	data.CreatedDate = convert.PtrTimeToFramework(remote.GetCreatedDate())
	// description is deliberately not read back: the API accepts it and never returns
	// it, so flattening would blank the configured value on every read. An
	// unset value still has to resolve, or the apply is rejected for leaving
	// it unknown.
	if data.Description.IsUnknown() {
		data.Description = types.StringNull()
	}
	// desired_status_code is deliberately not read back: the API accepts it and never returns
	// it, so flattening would blank the configured value on every read. An
	// unset value still has to resolve, or the apply is rejected for leaving
	// it unknown.
	if data.DesiredStatusCode.IsUnknown() {
		data.DesiredStatusCode = types.StringNull()
	}
	data.DistributedTracing = convert.PtrBoolToFramework(remote.GetDistributedTracing())
	// dns_override is deliberately not read back: the API accepts it and never returns
	// it, so flattening would blank the configured value on every read. An
	// unset value still has to resolve, or the apply is rejected for leaving
	// it unknown.
	if data.DNSOverride.IsUnknown() {
		data.DNSOverride = types.StringNull()
	}
	// download_limit is deliberately not read back: the API accepts it and never returns
	// it, so flattening would blank the configured value on every read. An
	// unset value still has to resolve, or the apply is rejected for leaving
	// it unknown.
	if data.DownloadLimit.IsUnknown() {
		data.DownloadLimit = types.Int64Null()
	}
	data.Enabled = convert.PtrBoolToFramework(remote.GetEnabled())
	// fixed_packet_rate is deliberately not read back: the API accepts it and never returns
	// it, so flattening would blank the configured value on every read. An
	// unset value still has to resolve, or the apply is rejected for leaving
	// it unknown.
	if data.FixedPacketRate.IsUnknown() {
		data.FixedPacketRate = types.Int64Null()
	}
	data.FollowRedirects = convert.PtrBoolToFramework(remote.GetFollowRedirects())
	// headers is deliberately not read back: the API accepts it and never returns
	// it, so flattening would blank the configured value on every read. An
	// unset value still has to resolve, or the apply is rejected for leaving
	// it unknown.
	if data.Headers.IsUnknown() {
		data.Headers = types.SetNull(types.StringType)
	}
	data.HTTPTargetTime = convert.PtrInt32ToFrameworkInt64(remote.GetHttpTargetTime())
	data.HTTPTimeLimit = convert.PtrInt32ToFrameworkInt64(remote.GetHttpTimeLimit())
	data.HTTPVersion = convert.PtrInt32ToFrameworkInt64(remote.GetHttpVersion())
	// include_headers is deliberately not read back: the API accepts it and never returns
	// it, so flattening would blank the configured value on every read. An
	// unset value still has to resolve, or the apply is rejected for leaving
	// it unknown.
	if data.IncludeHeaders.IsUnknown() {
		data.IncludeHeaders = types.BoolNull()
	}
	data.Interval = convert.PtrInt32ToFrameworkInt64(remote.GetInterval())
	data.Ipv6Policy = convert.PtrStringerToFramework(remote.GetIpv6Policy())
	data.LiveShare = convert.PtrBoolToFramework(remote.GetLiveShare())
	data.ModifiedBy = convert.PtrStringToFramework(remote.GetModifiedBy())
	data.ModifiedDate = convert.PtrTimeToFramework(remote.GetModifiedDate())
	data.MTUMeasurements = convert.PtrBoolToFramework(remote.GetMtuMeasurements())
	data.NetworkMeasurements = convert.PtrBoolToFramework(remote.GetNetworkMeasurements())
	data.NumPathTraces = convert.PtrInt32ToFrameworkInt64(remote.GetNumPathTraces())
	data.OverrideAgentProxy = convert.PtrBoolToFramework(remote.GetOverrideAgentProxy())
	data.OverrideProxyID = convert.PtrEnumToFramework(remote.GetOverrideProxyId())
	data.Password = convert.PtrEnumToFramework(remote.GetPassword())
	data.PathTraceMode = convert.PtrStringerToFramework(remote.GetPathTraceMode())
	// post_body is deliberately not read back: the API accepts it and never returns
	// it, so flattening would blank the configured value on every read. An
	// unset value still has to resolve, or the apply is rejected for leaving
	// it unknown.
	if data.PostBody.IsUnknown() {
		data.PostBody = types.StringNull()
	}
	data.ProbeMode = convert.PtrStringerToFramework(remote.GetProbeMode())
	data.Protocol = convert.PtrStringerToFramework(remote.GetProtocol())
	data.RandomizedStartTime = convert.PtrBoolToFramework(remote.GetRandomizedStartTime())
	data.RequestMethod = convert.PtrStringerToFramework(remote.GetRequestMethod())
	data.SavedEvent = convert.PtrBoolToFramework(remote.GetSavedEvent())
	data.SSLVersion = convert.PtrEnumToFramework(remote.GetSslVersion())
	data.SSLVersionID = convert.PtrStringerToFramework(remote.GetSslVersionId())
	data.ID = convert.PtrStringToFramework(remote.GetTestId())
	data.TestName = convert.PtrStringToFramework(remote.GetTestName())
	data.Type = convert.PtrStringerToFramework(remote.GetTypeEscaped())
	data.URL = convert.PtrEnumToFramework(remote.GetUrl())
	data.UseNtlm = convert.PtrBoolToFramework(remote.GetUseNtlm())
	data.UsePublicBGP = convert.PtrBoolToFramework(remote.GetUsePublicBgp())
	// user_agent is deliberately not read back: the API accepts it and never returns
	// it, so flattening would blank the configured value on every read. An
	// unset value still has to resolve, or the apply is rejected for leaving
	// it unknown.
	if data.UserAgent.IsUnknown() {
		data.UserAgent = types.StringNull()
	}
	data.Username = convert.PtrEnumToFramework(remote.GetUsername())
	data.VaultCredentials, d = flattenTestsHTTPServerTestVaultCredentials(ctx, remote.GetVaultCredentials())
	diags.Append(d...)
	data.VerifyCertificate = convert.PtrBoolToFramework(remote.GetVerifyCertificate())

	tflog.Debug(ctx, "mapped remote state", map[string]any{"resource": TypeName})

	return diags
}

// flattenTestsHTTPServerTestVaultCredentials converts SDK structs into the framework value state holds.
func flattenTestsHTTPServerTestVaultCredentials(ctx context.Context, in []models.Tests_API_TestVaultCredentialable) (types.Set, diag.Diagnostics) {
	var diags diag.Diagnostics

	// A nil slice is null and an empty slice is empty. Conflating them produces a
	// diff no configuration change can resolve.
	if in == nil {
		return types.SetNull(testsHTTPServerTestVaultCredentialObjectType), diags
	}

	elements := make([]TestsHTTPServerTestVaultCredentialModel, 0, len(in))
	for _, item := range in {
		var m TestsHTTPServerTestVaultCredentialModel

		m.SecretID = convert.PtrStringToFramework(item.GetSecretId())
		m.Target = convert.PtrStringerToFramework(item.GetTarget())
		elements = append(elements, m)
	}

	out, d2 := types.SetValueFrom(ctx, testsHTTPServerTestVaultCredentialObjectType, elements)
	diags.Append(d2...)

	return out, diags
}
