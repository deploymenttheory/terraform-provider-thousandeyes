// Code generated by tfpfgen from blueprints/thousandeyes
// (sha256:d986d7900b3714639519883166d0066510af34e4e4431384031a6f7b9dc0895a). DO NOT EDIT.

package tests_web_transaction_test

import (
	"os"
	"path/filepath"
	"testing"
	"time"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"

	"github.com/deploymenttheory/terraform-provider-thousandeyes/internal/acceptance"
	"github.com/deploymenttheory/terraform-provider-thousandeyes/internal/acceptance/check"
	"github.com/deploymenttheory/terraform-provider-thousandeyes/internal/acceptance/destroy"
	"github.com/deploymenttheory/terraform-provider-thousandeyes/internal/services/resources/web_transaction_tests/v7/tests_web_transaction"
)

// The resource under test, and the helper that asks the API whether it is really there.
const address = "thousandeyes_tests_web_transaction.test"

var testResource = tests_web_transaction.TestsWebTransactionTestResource{}

// TestAccResourceTestsWebTransaction_01_Lifecycle exercises the whole lifecycle against a live API.
//
// Guarded by TF_ACC, which terraform-plugin-testing requires, so `go test ./...` never reaches a
// live tenant by accident. The credential comes from the environment; see internal/acceptance.
//
// Every step ends with an implicit plan, and terraform-plugin-testing fails the step if that plan
// is not empty. That is the single strongest signal available that state mapping is right: an
// attribute the provider stores differently from how the API reports it shows up here as a diff on
// a resource nobody touched, and no amount of compiling or schema checking can see it.
func TestAccResourceTestsWebTransaction_01_Lifecycle(t *testing.T) {
	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { acceptance.PreCheck(t) },
		ProtoV6ProviderFactories: acceptance.ProtoV6ProviderFactories,
		// Destroy is verified against the API rather than against state, because Terraform
		// considers a destroy done when the provider's Delete returns no error -- which is a
		// statement about the request, not about the object.
		//
		// The wait is the built-in default; this API's consistency after delete was never measured,
		// so this is a budget rather than an observation
		CheckDestroy: destroy.CheckDestroyed(testResource, "thousandeyes_tests_web_transaction", 20*time.Second),
		Steps: []resource.TestStep{
			// Create, from the smallest configuration the schema permits.
			{
				Config: config(t, "minimal.tf"),
				Check: resource.ComposeAggregateTestCheckFunc(
					check.That(address).ExistsInAPI(testResource),
					check.That(address).Key("id").Exists(),
					check.That(address).Key("transaction_script").HasValue("tfacc-transaction-script"),
				),
			},
			// Import, then compare every attribute against the state the apply produced.
			//
			// This is what catches a read that populates state differently from a create, which
			// is the most common way a generated provider is subtly wrong.
			{
				ResourceName:      address,
				ImportState:       true,
				ImportStateVerify: true,
			},
			// Update in place, to a configuration that sets the optional attributes too.
			{
				Config: config(t, "maximal.tf"),
				Check: resource.ComposeAggregateTestCheckFunc(
					check.That(address).ExistsInAPI(testResource),
					check.That(address).Key("id").Exists(),
					check.That(address).Key("alerts_enabled").HasValue("true"),
					check.That(address).Key("allow_geolocation").HasValue("true"),
					check.That(address).Key("allow_mic_and_camera").HasValue("true"),
					check.That(address).Key("allow_unsafe_legacy_renegotiation").HasValue("true"),
					check.That(address).Key("auth_type").HasValue("none"),
					check.That(address).Key("bandwidth_measurements").HasValue("true"),
					check.That(address).Key("bgp_measurements").HasValue("true"),
					check.That(address).Key("block_domains").HasValue("tfacc-block-domains"),
					check.That(address).Key("browser_language").HasValue("tfacc-browser-language"),
					check.That(address).Key("chrome_options").HasValue("tfacc-chrome-options"),
					check.That(address).Key("chrome_policies").HasValue("tfacc-chrome-policies"),
					check.That(address).Key("chromium_track").HasValue("stable"),
					check.That(address).Key("collect_proxy_network_data").HasValue("true"),
					check.That(address).Key("content_regex").HasValue("tfacc-content-regex"),
					check.That(address).Key("description").HasValue("tfacc-description"),
					check.That(address).Key("desired_status_code").HasValue("tfacc-desired-status-code"),
					check.That(address).Key("disable_screenshot").HasValue("true"),
					check.That(address).Key("distributed_tracing").HasValue("true"),
					check.That(address).Key("dns_override").HasValue("192.0.2.1"),
					check.That(address).Key("download_limit").HasValue("1"),
					check.That(address).Key("enabled").HasValue("true"),
					check.That(address).Key("fixed_packet_rate").HasValue("1"),
					check.That(address).Key("flag_collect_console_logs").HasValue("true"),
					check.That(address).Key("follow_redirects").HasValue("true"),
					check.That(address).Key("http_target_time").HasValue("100"),
					check.That(address).Key("http_time_limit").HasValue("5"),
					check.That(address).Key("http_version").HasValue("1"),
					check.That(address).Key("identify_agent_traffic_with_user_agent").HasValue("true"),
					check.That(address).Key("include_headers").HasValue("true"),
					check.That(address).Key("interval").HasValue("60"),
					check.That(address).Key("mtu_measurements").HasValue("true"),
					check.That(address).Key("network_measurements").HasValue("true"),
					check.That(address).Key("num_path_traces").HasValue("1"),
					check.That(address).Key("override_agent_proxy").HasValue("true"),
					check.That(address).Key("page_loading_strategy").HasValue("normal"),
					check.That(address).Key("path_trace_mode").HasValue("classic"),
					check.That(address).Key("probe_mode").HasValue("auto"),
					check.That(address).Key("protocol").HasValue("tcp"),
					check.That(address).Key("randomized_start_time").HasValue("true"),
					check.That(address).Key("ssl_version_id").HasValue("0"),
					check.That(address).Key("subinterval").HasValue("60"),
					check.That(address).Key("target_time").HasValue("1"),
					check.That(address).Key("test_name").HasValue("tfacc-test-name"),
					check.That(address).Key("time_limit").HasValue("5"),
					check.That(address).Key("transaction_script").HasValue("tfacc-transaction-script"),
					check.That(address).Key("url").HasValue("tfacc-url"),
					check.That(address).Key("use_ntlm").HasValue("true"),
					check.That(address).Key("use_public_bgp").HasValue("true"),
					check.That(address).Key("user_agent").HasValue("tfacc-user-agent"),
					check.That(address).Key("verify_certificate").HasValue("true"),
				),
			},
		},
	})
}

// config reads one of the committed fixtures.
//
// From testdata rather than from a string constant in this file, so the configuration can be
// edited and re-run without recompiling, and so `terraform fmt` can police it.
func config(t *testing.T, name string) string {
	t.Helper()

	body, err := os.ReadFile(filepath.Join("testdata", name))
	if err != nil {
		t.Fatalf("reading fixture %s: %v", name, err)
	}

	return string(body)
}
