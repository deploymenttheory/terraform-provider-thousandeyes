// Code generated by tfpfgen from blueprints/thousandeyes
// (sha256:5784e03978dc4aa1168b63f15b5200e4d2c21e162af10c9c1d3532b5a6e56762). DO NOT EDIT.

package tag_test

import (
	"testing"
	"time"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"
	"github.com/hashicorp/terraform-plugin-testing/querycheck"
	"github.com/hashicorp/terraform-plugin-testing/tfversion"

	"github.com/deploymenttheory/terraform-provider-thousandeyes/internal/acceptance"
	"github.com/deploymenttheory/terraform-provider-thousandeyes/internal/acceptance/destroy"
)

// TestAccListTag_01_Query seeds one thousandeyes_tag and asserts `terraform query` finds it.
//
// The identity stream is the workflow under test -- query results feed `terraform plan
// -generate-config-out` -- and the seeded object is the one element known to exist, so
// the assertion is a floor rather than an exact count: the tenant may legitimately hold
// more. address, testResource and config() are the resource acceptance test's own
// declarations; both files share a package by design.
func TestAccListTag_01_Query(t *testing.T) {
	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { acceptance.PreCheck(t) },
		ProtoV6ProviderFactories: acceptance.ProtoV6ProviderFactories,
		// Query needs a modern CLI; skipping is stated, not silent.
		TerraformVersionChecks: []tfversion.TerraformVersionCheck{
			tfversion.SkipBelow(tfversion.Version1_14_0),
		},
		//
		// The wait is the built-in default; this API's consistency after delete was never measured,
		// so this is a budget rather than an observation
		CheckDestroy: destroy.CheckDestroyed(testResource, "thousandeyes_tag", 20*time.Second),
		Steps: []resource.TestStep{
			// Seed: the query below must have at least this object to find.
			{
				Config: config(t, "minimal.tf"),
			},
			{
				Query:  true,
				Config: config(t, "query.tf"),
				QueryResultChecks: []querycheck.QueryResultCheck{
					querycheck.ExpectLengthAtLeast("thousandeyes_tag.test", 1),
				},
			},
		},
	})
}
