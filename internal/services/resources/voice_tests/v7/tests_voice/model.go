// Code generated by tfpfgen from blueprints/thousandeyes
// (sha256:5bb9a56c6d082987640c8d4049827cbb3511cd53115e02b95bc05bd352cf8a1b). DO NOT EDIT.

package tests_voice

import (
	"github.com/hashicorp/terraform-plugin-framework-timeouts/resource/timeouts"
	"github.com/hashicorp/terraform-plugin-framework/attr"
	"github.com/hashicorp/terraform-plugin-framework/types"
)

// TestsVoiceResourceModel is the Terraform state for a thousandeyes_tests_voice.
//
// Field order follows the schema, and the timeouts value is last.
type TestsVoiceResourceModel struct {
	AlertsEnabled       types.Bool     `tfsdk:"alerts_enabled"`
	BGPMeasurements     types.Bool     `tfsdk:"bgp_measurements"`
	Codec               types.String   `tfsdk:"codec"`
	CodecID             types.String   `tfsdk:"codec_id"`
	CreatedBy           types.String   `tfsdk:"created_by"`
	CreatedDate         types.String   `tfsdk:"created_date"`
	Description         types.String   `tfsdk:"description"`
	Dscp                types.String   `tfsdk:"dscp"`
	DscpID              types.String   `tfsdk:"dscp_id"`
	Duration            types.Int64    `tfsdk:"duration"`
	Enabled             types.Bool     `tfsdk:"enabled"`
	ID                  types.String   `tfsdk:"id"`
	Interval            types.Int64    `tfsdk:"interval"`
	JitterBuffer        types.Int64    `tfsdk:"jitter_buffer"`
	Labels              types.Set      `tfsdk:"labels"`
	LiveShare           types.Bool     `tfsdk:"live_share"`
	ModifiedBy          types.String   `tfsdk:"modified_by"`
	ModifiedDate        types.String   `tfsdk:"modified_date"`
	Monitors            types.Set      `tfsdk:"monitors"`
	NumPathTraces       types.Int64    `tfsdk:"num_path_traces"`
	Port                types.Int64    `tfsdk:"port"`
	RandomizedStartTime types.Bool     `tfsdk:"randomized_start_time"`
	SavedEvent          types.Bool     `tfsdk:"saved_event"`
	SharedWithAccounts  types.Set      `tfsdk:"shared_with_accounts"`
	Tags                types.Set      `tfsdk:"tags"`
	TargetAgentID       types.String   `tfsdk:"target_agent_id"`
	TestName            types.String   `tfsdk:"test_name"`
	Type                types.String   `tfsdk:"type"`
	UsePublicBGP        types.Bool     `tfsdk:"use_public_bgp"`
	Timeouts            timeouts.Value `tfsdk:"timeouts"`
}

// TestsVoiceTestLabelsModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type TestsVoiceTestLabelsModel struct {
	IsBuiltin types.Bool   `tfsdk:"is_builtin"`
	LabelID   types.String `tfsdk:"label_id"`
	Name      types.String `tfsdk:"name"`
}

// testsVoiceTestLabelsAttrTypes describes TestsVoiceTestLabelsModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var testsVoiceTestLabelsAttrTypes = map[string]attr.Type{
	"is_builtin": types.BoolType,
	"label_id":   types.StringType,
	"name":       types.StringType,
}

var testsVoiceTestLabelsObjectType = types.ObjectType{AttrTypes: testsVoiceTestLabelsAttrTypes}

// TestsVoiceTestBGPMonitorsModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type TestsVoiceTestBGPMonitorsModel struct {
	CountryID   types.String `tfsdk:"country_id"`
	IPAddress   types.String `tfsdk:"ip_address"`
	MonitorID   types.String `tfsdk:"monitor_id"`
	MonitorName types.String `tfsdk:"monitor_name"`
	MonitorType types.String `tfsdk:"monitor_type"`
	Network     types.String `tfsdk:"network"`
}

// testsVoiceTestBGPMonitorsAttrTypes describes TestsVoiceTestBGPMonitorsModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var testsVoiceTestBGPMonitorsAttrTypes = map[string]attr.Type{
	"country_id":   types.StringType,
	"ip_address":   types.StringType,
	"monitor_id":   types.StringType,
	"monitor_name": types.StringType,
	"monitor_type": types.StringType,
	"network":      types.StringType,
}

var testsVoiceTestBGPMonitorsObjectType = types.ObjectType{AttrTypes: testsVoiceTestBGPMonitorsAttrTypes}

// TestsVoiceTestSharedAccountsModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type TestsVoiceTestSharedAccountsModel struct {
	AID  types.String `tfsdk:"aid"`
	Name types.String `tfsdk:"name"`
}

// testsVoiceTestSharedAccountsAttrTypes describes TestsVoiceTestSharedAccountsModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var testsVoiceTestSharedAccountsAttrTypes = map[string]attr.Type{
	"aid":  types.StringType,
	"name": types.StringType,
}

var testsVoiceTestSharedAccountsObjectType = types.ObjectType{AttrTypes: testsVoiceTestSharedAccountsAttrTypes}

// TestsVoiceTestTagsModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type TestsVoiceTestTagsModel struct {
	ID    types.String `tfsdk:"id"`
	Key   types.String `tfsdk:"key"`
	Value types.String `tfsdk:"value"`
}

// testsVoiceTestTagsAttrTypes describes TestsVoiceTestTagsModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var testsVoiceTestTagsAttrTypes = map[string]attr.Type{
	"id":    types.StringType,
	"key":   types.StringType,
	"value": types.StringType,
}

var testsVoiceTestTagsObjectType = types.ObjectType{AttrTypes: testsVoiceTestTagsAttrTypes}
