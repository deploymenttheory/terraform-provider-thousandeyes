// Code generated by tfpfgen from blueprints/thousandeyes
// (sha256:17ccadd9f04833a0c47aa480004eff43ce664c1e4202c4da5c92d766e70cb2c7). DO NOT EDIT.

package stream

import (
	"github.com/hashicorp/terraform-plugin-framework-timeouts/resource/timeouts"
	"github.com/hashicorp/terraform-plugin-framework/attr"
	"github.com/hashicorp/terraform-plugin-framework/types"
)

// StreamResourceModel is the Terraform state for a thousandeyes_stream.
//
// Field order follows the schema, and the timeouts value is last.
type StreamResourceModel struct {
	Enabled            types.Bool     `tfsdk:"enabled"`
	EndpointAgentLabel types.Set      `tfsdk:"endpoint_agent_label"`
	EndpointAgentTag   types.Set      `tfsdk:"endpoint_agent_tag"`
	ExporterConfig     types.Object   `tfsdk:"exporter_config"`
	Filters            types.Object   `tfsdk:"filters"`
	ID                 types.String   `tfsdk:"id"`
	InputConfig        types.Object   `tfsdk:"input_config"`
	StreamEndpointURL  types.String   `tfsdk:"stream_endpoint_url"`
	StreamStatus       types.Object   `tfsdk:"stream_status"`
	TagMatch           types.Set      `tfsdk:"tag_match"`
	TestMatch          types.Set      `tfsdk:"test_match"`
	Timeouts           timeouts.Value `tfsdk:"timeouts"`
}

// StreamEndpointAgentLabelModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamEndpointAgentLabelModel struct {
	ID types.String `tfsdk:"id"`
}

// streamEndpointAgentLabelAttrTypes describes StreamEndpointAgentLabelModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamEndpointAgentLabelAttrTypes = map[string]attr.Type{
	"id": types.StringType,
}

var streamEndpointAgentLabelObjectType = types.ObjectType{AttrTypes: streamEndpointAgentLabelAttrTypes}

// StreamEndpointAgentTagModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamEndpointAgentTagModel struct {
	ID types.String `tfsdk:"id"`
}

// streamEndpointAgentTagAttrTypes describes StreamEndpointAgentTagModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamEndpointAgentTagAttrTypes = map[string]attr.Type{
	"id": types.StringType,
}

var streamEndpointAgentTagObjectType = types.ObjectType{AttrTypes: streamEndpointAgentTagAttrTypes}

// StreamExporterConfigModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamExporterConfigModel struct {
	Authorization types.Object `tfsdk:"authorization"`
	SplunkHec     types.Object `tfsdk:"splunk_hec"`
}

// streamExporterConfigAttrTypes describes StreamExporterConfigModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamExporterConfigAttrTypes = map[string]attr.Type{
	"authorization": streamExporterConfigAuthorizationObjectType,
	"splunk_hec":    streamExporterConfigSplunkHecObjectType,
}

var streamExporterConfigObjectType = types.ObjectType{AttrTypes: streamExporterConfigAttrTypes}

// StreamExporterConfigAuthorizationModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamExporterConfigAuthorizationModel struct {
	OAuth2 types.Object `tfsdk:"o_auth2"`
}

// streamExporterConfigAuthorizationAttrTypes describes StreamExporterConfigAuthorizationModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamExporterConfigAuthorizationAttrTypes = map[string]attr.Type{
	"o_auth2": streamExporterConfigOAuth2ObjectType,
}

var streamExporterConfigAuthorizationObjectType = types.ObjectType{AttrTypes: streamExporterConfigAuthorizationAttrTypes}

// StreamExporterConfigOAuth2Model is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamExporterConfigOAuth2Model struct {
	ClientID     types.String `tfsdk:"client_id"`
	ClientSecret types.String `tfsdk:"client_secret"`
	Scopes       types.Set    `tfsdk:"scopes"`
	TokenURL     types.String `tfsdk:"token_url"`
}

// streamExporterConfigOAuth2AttrTypes describes StreamExporterConfigOAuth2Model to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamExporterConfigOAuth2AttrTypes = map[string]attr.Type{
	"client_id":     types.StringType,
	"client_secret": types.StringType,
	"scopes":        types.SetType{ElemType: types.StringType},
	"token_url":     types.StringType,
}

var streamExporterConfigOAuth2ObjectType = types.ObjectType{AttrTypes: streamExporterConfigOAuth2AttrTypes}

// StreamExporterConfigSplunkHecModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamExporterConfigSplunkHecModel struct {
	Index      types.String `tfsdk:"index"`
	Source     types.String `tfsdk:"source"`
	SourceType types.String `tfsdk:"source_type"`
	Token      types.String `tfsdk:"token"`
}

// streamExporterConfigSplunkHecAttrTypes describes StreamExporterConfigSplunkHecModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamExporterConfigSplunkHecAttrTypes = map[string]attr.Type{
	"index":       types.StringType,
	"source":      types.StringType,
	"source_type": types.StringType,
	"token":       types.StringType,
}

var streamExporterConfigSplunkHecObjectType = types.ObjectType{AttrTypes: streamExporterConfigSplunkHecAttrTypes}

// StreamFiltersModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamFiltersModel struct {
	TestTypes types.Object `tfsdk:"test_types"`
}

// streamFiltersAttrTypes describes StreamFiltersModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamFiltersAttrTypes = map[string]attr.Type{
	"test_types": streamFiltersTestTypesObjectType,
}

var streamFiltersObjectType = types.ObjectType{AttrTypes: streamFiltersAttrTypes}

// StreamFiltersTestTypesModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamFiltersTestTypesModel struct {
	Values types.Set `tfsdk:"values"`
}

// streamFiltersTestTypesAttrTypes describes StreamFiltersTestTypesModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamFiltersTestTypesAttrTypes = map[string]attr.Type{
	"values": types.SetType{ElemType: types.StringType},
}

var streamFiltersTestTypesObjectType = types.ObjectType{AttrTypes: streamFiltersTestTypesAttrTypes}

// StreamInputConfigModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamInputConfigModel struct {
	Metric types.Object `tfsdk:"metric"`
}

// streamInputConfigAttrTypes describes StreamInputConfigModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamInputConfigAttrTypes = map[string]attr.Type{
	"metric": streamInputConfigMetricObjectType,
}

var streamInputConfigObjectType = types.ObjectType{AttrTypes: streamInputConfigAttrTypes}

// StreamInputConfigMetricModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamInputConfigMetricModel struct {
	ConnectedDevices types.Object `tfsdk:"connected_devices"`
}

// streamInputConfigMetricAttrTypes describes StreamInputConfigMetricModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamInputConfigMetricAttrTypes = map[string]attr.Type{
	"connected_devices": streamConnectedDevicesObjectType,
}

var streamInputConfigMetricObjectType = types.ObjectType{AttrTypes: streamInputConfigMetricAttrTypes}

// StreamConnectedDevicesModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamConnectedDevicesModel struct {
	Enabled types.Bool `tfsdk:"enabled"`
}

// streamConnectedDevicesAttrTypes describes StreamConnectedDevicesModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamConnectedDevicesAttrTypes = map[string]attr.Type{
	"enabled": types.BoolType,
}

var streamConnectedDevicesObjectType = types.ObjectType{AttrTypes: streamConnectedDevicesAttrTypes}

// StreamStatusModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamStatusModel struct {
	LastFailure types.Int64  `tfsdk:"last_failure"`
	LastSuccess types.Int64  `tfsdk:"last_success"`
	Status      types.String `tfsdk:"status"`
}

// streamStatusAttrTypes describes StreamStatusModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamStatusAttrTypes = map[string]attr.Type{
	"last_failure": types.Int64Type,
	"last_success": types.Int64Type,
	"status":       types.StringType,
}

var streamStatusObjectType = types.ObjectType{AttrTypes: streamStatusAttrTypes}

// StreamTagMatchModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamTagMatchModel struct {
	Key   types.String `tfsdk:"key"`
	Value types.String `tfsdk:"value"`
}

// streamTagMatchAttrTypes describes StreamTagMatchModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamTagMatchAttrTypes = map[string]attr.Type{
	"key":   types.StringType,
	"value": types.StringType,
}

var streamTagMatchObjectType = types.ObjectType{AttrTypes: streamTagMatchAttrTypes}

// StreamTestMatchModel is one element of a nested attribute.
//
// It is a sibling of the resource model rather than an inner type, because the
// framework decodes collection elements into a named type.
type StreamTestMatchModel struct {
	Domain types.String `tfsdk:"domain"`
	ID     types.String `tfsdk:"id"`
}

// streamTestMatchAttrTypes describes StreamTestMatchModel to the framework.
//
// It is declared once and referenced by both conversion helpers, so the shape
// cannot drift between the direction that reads it and the direction that writes it.
var streamTestMatchAttrTypes = map[string]attr.Type{
	"domain": types.StringType,
	"id":     types.StringType,
}

var streamTestMatchObjectType = types.ObjectType{AttrTypes: streamTestMatchAttrTypes}
