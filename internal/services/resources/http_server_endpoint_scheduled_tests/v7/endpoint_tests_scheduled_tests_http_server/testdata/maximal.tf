# Code generated by tfpfgen from blueprints/thousandeyes
# (sha256:0705a06d171a00d2bf9756d028fa63d1589968e3dae11ee9272c1c4039c2d00f). DO NOT EDIT.

# The widest configuration the evidence supports: every writable attribute with a derivable
# value, chosen by the same derivation the rehearsal probe sends to the live API.
#
# Regenerated and policed, deliberately. This file used to be a hand-owned scaffold, and
# hand edits are how its values drifted from the generated assertions -- the checks derive
# from the blueprint, so the blueprint is where a value is corrected: an accFixture hint for
# something only a human knows, recorded behaviour for everything the probe observed. What
# could not be derived is listed inside the block, each with the reason.

resource "thousandeyes_endpoint_tests_http_server" "test" {
  # Where a value came from.
  #
  # interval: curated in the blueprint's accFixture; the generator cannot derive it
  #
  # protocol: documented; unprobed
  #
  # tcp_probe_mode: documented; unprobed
  #
  # url: curated in the blueprint's accFixture; the generator cannot derive it
  #
  # Not filled in, because no correct value could be derived. Each of these is optional, so this
  # configuration is valid as it stands -- but it is testing less than it could.
  #
  # agent_selector_type: immutable; setting it at update would force replacement, so it belongs
  # in the create configuration or nowhere
  #
  # agents: immutable; setting it at update would force replacement, so it belongs in the create
  # configuration or nowhere
  #
  # auth_type: immutable; setting it at update would force replacement, so it belongs in the
  # create configuration or nowhere
  #
  # endpoint_agent_labels: immutable; setting it at update would force replacement, so it
  # belongs in the create configuration or nowhere
  #
  # has_path_trace_in_session: immutable; setting it at update would force replacement, so it
  # belongs in the create configuration or nowhere
  #
  # has_ping: immutable; setting it at update would force replacement, so it belongs in the
  # create configuration or nowhere
  #
  # has_traceroute: immutable; setting it at update would force replacement, so it belongs in
  # the create configuration or nowhere
  #
  # http_time_limit: immutable; setting it at update would force replacement, so it belongs in
  # the create configuration or nowhere
  #
  # ip_version: immutable; setting it at update would force replacement, so it belongs in the
  # create configuration or nowhere
  #
  # is_prioritized: immutable; setting it at update would force replacement, so it belongs in
  # the create configuration or nowhere
  #
  # max_machines: immutable; setting it at update would force replacement, so it belongs in the
  # create configuration or nowhere
  #
  # network_measurements: immutable; setting it at update would force replacement, so it belongs
  # in the create configuration or nowhere
  #
  # password: immutable; setting it at update would force replacement, so it belongs in the
  # create configuration or nowhere
  #
  # ssl_version_id: immutable; setting it at update would force replacement, so it belongs in
  # the create configuration or nowhere
  #
  # tag_ids: immutable; setting it at update would force replacement, so it belongs in the
  # create configuration or nowhere
  #
  # target_response_time: immutable; setting it at update would force replacement, so it belongs
  # in the create configuration or nowhere
  #
  # username: immutable; setting it at update would force replacement, so it belongs in the
  # create configuration or nowhere
  #
  # verify_certificate: immutable; setting it at update would force replacement, so it belongs
  # in the create configuration or nowhere
  interval       = "600"
  protocol       = "icmp"
  tcp_probe_mode = "auto"
  test_name      = "tfacc-test-name"
  url            = "https://www.example.com"
}
